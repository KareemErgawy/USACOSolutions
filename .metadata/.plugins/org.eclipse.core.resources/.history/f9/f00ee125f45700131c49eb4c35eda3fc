/*
 ID: kareem_2
 PROG: ariprog
 LANG: C++
 */
#include <iostream>
using namespace std;

int* bisquares;

void generateBisquares(int m) {
	bisquares = new int[m * m * 2];
	for (int i = 0; i <= m; i++) {
		for (int j = 0; j <= m; j++) {
			bisquares[(i * i) + (j * j)];
		}
	}
}

int main() {
	generateBisquares(7);

	return 0;
}
