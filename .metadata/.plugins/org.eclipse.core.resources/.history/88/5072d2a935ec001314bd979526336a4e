/*
 ID: kareem_2
 PROG: kimbits
 LANG: C++
 */
#include <iostream>
#include <fstream>
#include <queue>
#include <set>
using namespace std;

long next(long x){
	long b = x & -x;
	long t = x + b;
	long c = x ^ t;
	long m = (c >> 2) / b;
	return t | m;
}

int main() {

	ifstream fin("kimbits.in");
	ofstream fout("kimbits.out");

	int n, l, i;

	fin >> n >> l >> i;

	set<long, greater<long> > undiscovered;
	undiscovered.insert(1);

	long smallest;
	for (int j = 0; j < i; j++) {
		smallest = *undiscovered.begin();
		long nextNum = next(smallest);
		undiscovered.insert(nextNum);
		undiscovered.erase(nextNum);
	}

	cout << smallest << endl;

	return 0;
}
