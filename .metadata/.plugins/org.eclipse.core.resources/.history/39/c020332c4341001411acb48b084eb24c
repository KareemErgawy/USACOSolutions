/*
 ID: kareem_2
 PROG: sort3
 LANG: C++
 */
#include <iostream>
#include <fstream>
#include <algorithm>
using namespace std;

int main() {
	ifstream fin("sort3.in");
	ofstream fout("sort3.out");
	int n;
	fin >> n;
	int* input = new int[n];
	int* sorted = new int[n];

	for(int i=0 ; i<n ; i++)
	{
		fin >> input[i];
		sorted[i] = input[i];
	}

	sort(sorted, sorted + n);

	return 0;
}
